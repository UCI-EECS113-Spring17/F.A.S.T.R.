To run the Python benchmark, use the command:
  "python3 matmult.py <n>"
  
To build the C benchmark, use the "make" command.

To run the C benchmark, use the command:
  "./matmult <n>"
  
For both benchmarks, the argument n is the size of the square matrices that
will be generated and multiplied.
